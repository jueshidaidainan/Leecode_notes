栈的这几道题，多是括号运算符匹配或者单调栈的问题。栈的性质是先进后出，进去再出来会反过来。
那考虑到用栈，一般就是考虑这道题涉及到局部性的最大值呀，最小值呀，这就是单调栈的特点。
也就是设定一个规则，什么时候出栈，什么时候入栈，栈里保存的都是符合局部性质的元素，最后处理栈不为空的特殊元素。
括号匹配也是，每个右括号其实是匹配最近的左括号，所以遇见左括号入栈，右括号出栈，就很显然了。当然涉及数字的时候，可以考虑使用数字栈和字母栈的双栈来处理。
我们知道栈是计算机函数调用和底层结构里使用的结构，所以很多时候可以类比着看。